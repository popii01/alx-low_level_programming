#include<stdio.h>
/**
 * main - for entry points
 * Return: 0
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'a') {
			if ((c != 'q' && c != 'e') && c <= 'z')
				putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
